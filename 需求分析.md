# Chrome 侧边栏 AI 网页总结扩展 —— 需求分析

- 日期：2026-09-02（已按用户确认更新）
- 目标平台：Windows + Chrome（扩展本身跨平台，但面向 Chrome）
- 技术形态：Manifest V3 扩展（Chrome 114+ 支持 Side Panel API）

---

## 9. 需求变更 V2（2026-09-02，已实现）

| # | 变更 | 说明 |
|---|------|------|
| V1 | 自定义总结提示词 | 设置页新增"总结提示词"编辑框，默认"请返回您反复阅读正文后精心写成的详尽笔记"；清空则回退该默认值 |
| V2 | "总结中"居中显示 | 提取/总结阶段在侧边栏中部显示状态（居中卡片/动画），流式输出开始后转为顶部正文排版 |
| V3 | 更换图标 | 使用用户提供的 D:\logo.png，用 PowerShell + System.Drawing（.NET 内置）缩放生成 16/48/128 三档 PNG 覆盖 icons/ |
| V4 | 页面白名单 | 仅 http://、https://、file:// 页面可总结；其他页面（chrome://、扩展商店、PDF 等）侧边栏提示"请在网页上使用"+ chrome.notifications 弹系统通知（需新增 notifications 权限） |
| V5 | bilibili 总结 | 视频页走专用流程：注入 MAIN world 脚本 -> web-interface/view（标题/简介/cid）+ x/player/v2（字幕列表，均 credentials:"include" 带 B 站 cookie）-> 下载自动字幕 JSON -> 组装 Markdown（标题/UP主/简介/字幕段落）-> 送 AI 总结 |

**V5 关键设计与前提：**
1. 必须在页面上下文（MAIN world）fetch：api.bilibili.com 与 www.bilibili.com 同站（bilibili.com），页面上下文请求才能带上 .bilibili.com 的 cookie；扩展页面自身 fetch 无法携带站点 cookie。
2. **自动字幕通常需要 B 站登录态**（未登录多数视频无字幕列表）；无字幕时降级为"提示未获取到字幕 + 标题/简介 + 页面正文兜底"继续总结。
3. 字幕 JSON 下载在页面上下文完成，自动携带 Referer 与 cookie；若被 CORS 拦截，兜底方案为扩展后台带 Referer 请求。
4. 接口风控：正常使用频率无碍；若返回 -412/-352 等风控码，给出中文提示。
5. 识别范围：www.bilibili.com/video/(BVxxx 或 avxxx)；番剧/直播/专栏页暂不支持。

**V4 注意**：file:// 页面需用户到扩展详情页开启"允许访问文件网址"，否则无法注入；README 中说明，注入失败时提示语说明原因。

---

## 1. 产品定位

在 Chrome 侧边栏中，一键调用 AI 总结当前网页内容。用户可点击扩展图标或按快捷键触发，结果流式显示在侧边栏中。

## 2. 功能需求

### P0 —— 核心功能

| # | 模块 | 说明 |
|---|------|------|
| F1 | 网页内容提取 | 提取当前标签页的正文（排除导航、广告、页脚等噪声），支持长网页截断 |
| F2 | AI 总结 | 调 API 生成总结，**流式输出**（边生成边显示）；关闭侧边栏即中断 |
| F3 | 触发方式 A：点击图标 | 点击扩展图标 → 打开侧边栏并自动总结当前页 |
| F4 | 触发方式 B：快捷键 | 按快捷键 → 打开侧边栏并自动总结当前页（快捷键可在 Chrome 快捷键页自定义） |
| F5 | OpenAI 兼容 API 配置 | 可填 API Base URL（默认可改，兼容 DeepSeek/Kimi/通义等第三方）+ API Key |
| F6 | Gemini API 配置 | 官方 `generativelanguage.googleapis.com` + API Key |
| F7 | 自动拉取模型列表 | 配置页点"获取模型"，调用 models 接口拉取，下拉框选择默认模型；接口拉不到时支持手动输入模型名 |
| F8 | 检测模型可用性 | 配置页"测试连接"按钮：① 验证 Key/接口（拉模型列表）② 对所选模型发一个 `max_tokens=1` 的最小请求验证真实可调用，结果分步提示 |
| F9 | 字体大小设置 | 侧边栏总结文字的可调节字号（如 12–20px），持久化 |
| F10 | 配置持久化 | 所有设置存入 `chrome.storage.local`（含 API Key，仅本机，不上传） |
| F11 | 设置入口 | 右键扩展图标 → "选项"（Chrome 内建菜单，声明 `options_ui` 即自动出现），为唯一入口 |

### P1 —— 明确不做（用户已确认）

总结模式预设、自定义提示词、总结历史记录、复制/导出、选中文本总结、深色模式；侧边栏不设底部功能按钮区（总结/重新总结、复制、清空均不做）。

## 3. 界面规划

**侧边栏（sidepanel.html）**
- 仅一个总结文字区：流式渲染总结文字；空态显示引导文案；错误态显示错误提示；无任何顶部栏/按钮区
- 使用的模型为设置页中选定的默认模型
- 总结由点击图标 / 快捷键触发（打开即自动总结）；重新总结可再次触发

**设置页（options.html，以独立标签页打开）**
- 两个配置分组：OpenAI 兼容、Gemini
- 每组：Base URL、API Key（密码框+显示切换）、获取模型按钮、模型下拉、测试连接按钮及结果状态
- 通用：字体大小滑块、快捷键说明（提示去 `chrome://extensions/shortcuts` 修改）、保存按钮
- 打开方式：右键扩展图标 → "选项"；或侧边栏齿轮按钮

## 4. 技术架构

```
┌─────────────── Chrome 扩展 (Manifest V3) ───────────────┐
│  background (Service Worker)                            │
│  · setPanelBehavior({openPanelOnActionClick:true})      │
│  · 监听 chrome.commands 快捷键 → sidePanel.open + 触发总结 │
│  · 发送消息通知 sidepanel 开始总结                        │
│                                                          │
│  sidepanel.html/js                                       │
│  · chrome.tabs.query 取当前标签页                          │
│  · chrome.scripting.executeScript 注入提取函数取正文      │
│  · fetch AI API（流式 ReadableStream）渲染到界面          │
│  · 读写 chrome.storage.local                             │
│                                                          │
│  options.html/js（设置页）                                │
│  · 拉取模型 / 测试连接 / 保存设置                          │
│                                                          │
│  manifest.json                                           │
│  permissions: sidePanel, storage, activeTab, scripting,  │
│               commands, tabs                             │
│  host_permissions: https://*/*  http://*/*               │
│  options_ui: { page: "options.html", open_in_tab: true } │
│  commands: _execute_action + summarize-page(Ctrl+Shift+U)│
└──────────────────────────────────────────────────────────┘
```

**关键设计决策：**

1. **Provider 抽象**：OpenAI 兼容与 Gemini 各配置一组（type/baseUrl/apiKey/model），请求层封装两种路径：
   - OpenAI 兼容：`GET {base}/models`（拉模型）、`POST {base}/chat/completions`（流式 chat）
   - Gemini：`GET https://generativelanguage.googleapis.com/v1beta/models?key=...`（拉模型）、`POST https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent?key=...`（流式）—— Gemini 也支持 OpenAI 兼容端点，架构上可复用 OpenAI 客户端，仅认证方式不同
   - 认证模式各留开关：`Authorization: Bearer` vs `?key=` / `x-goog-api-key`

2. **正文提取**：用 `activeTab` + `chrome.scripting.executeScript` 注入提取脚本（`article` 标签优先，否则 `body.innerText` 清洗），或直接内嵌 Mozilla Readability.js 提升正文识别质量；长文本按字符上限（如 3 万字符）做首尾截断。

3. **流式输出**：`fetch` + `ReadableStream` 逐 chunk 解析 SSE 并在侧边栏追加渲染；关闭面板时页面销毁、请求自动中断。

4. **设置入口**：manifest 声明 `options_ui` 后，右键扩展图标自动出现"选项"菜单项（内建行为，无需代码），是唯一设置入口。

5. **API Key 存储**：`chrome.storage.local`（不走 sync，避免同步到云端）；MV3 禁止远程代码，所有逻辑本地打包。

## 5. 技术约束与风险提示

| 注意点 | 说明 |
|--------|------|
| Chrome 版本 | Side Panel API 需 Chrome 114+（2026 年现状下没问题） |
| 快捷键不可程序化修改 | 扩展无法在设置页里直接改快捷键，只能 `chrome.commands` 声明 + 引导用户去 `chrome://extensions/shortcuts` 修改；命令总数上限 4 个 |
| CORS | 侧边栏/配置页（扩展页面）发跨域 fetch 需 host_permissions 覆盖目标 API 域名，用 `https://*/*` 通配兜底第三方 Base URL |
| 受限页面 | 浏览器内置 PDF 查看器、chrome:// 页面、商店页面等无法注入脚本，需给出明确提示 |
| 部分第三方 API 无 /models 接口 | 拉取失败时自动切换为手动输入模型名 |
| 长上下文付费 | 截断策略 + 在 UI 显示所传字符数 |
| Key 泄露风险 | 明文存本机 storage.local，属常见做法；如涉敏感 Key，后续可用 `chrome.storage.session` 或用户自托管方案升级 |

## 6. 待确认问题

已全部确认：P1 不做；界面语言为中文；两种 API 各一套配置，不做多套保存与切换。

## 7. 交付物

- 完整可加载的 MV3 扩展源码（load unpacked 即可用）
- 说明文档：安装方式、快捷键设置指引

## 8. 开发顺序建议

1. 骨架：manifest + 侧边栏 + 图标点击打开
2. 设置页 + 存储 + 模型拉取 + 测试连接
3. 正文提取 + 总结流式调用打通
4. 快捷键触发 + 字体大小
5. 打磨（错误提示、空态、加载态）
