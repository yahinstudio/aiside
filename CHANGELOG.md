# 更新日志

本文件记录对使用者有影响的变化，重点是 DeepSeek / Kimi 网页端协议的兼容调整与权限、隐私相关的
行为变化。更早的版本请查阅 git 历史。

## 未发布（工程加固）

本轮为工程质量与安全加固，不新增产品功能。**有多处行为变化，升级后请留意：**

### 行为变化

- **权限收窄。** 不再默认预授权所有 http/https 站点。网页内容读取改由 `activeTab` 承担——点击
  扩展图标或快捷键触发时有效；**切换标签页后在侧边栏内点「总结当前网页」不再直接可用**，会提示
  授权并给出一键授权入口。自定义 API 的域名需要在设置页保存 Base URL 时按 origin 授权。
- **Kimi 附件上传可能降级。** 附件上传目标由 Kimi 服务端签发，无法预先声明权限；被拒或附件超过
  2MB 时会退回内联文本模式，并在结果上方说明原因。
- **Kimi 附件内容更保守。** 生成附件时在原始 DOM 上过滤隐藏节点（`hidden`、`aria-hidden`、
  `display:none`），链接只保留 http/https 且去掉 query/hash。
- **凭据存放位置变化。** DeepSeek / Kimi 的 token 改存 `chrome.storage.session`，关闭浏览器即失效，
  下次使用会重新读取；旧版本已存于 `storage.local` 的凭据会在首次读取时自动迁移并删除。新增
  「记住 API Key」开关（默认开启）；关闭后 API Key 只存会话。
- **自定义 API 的 Base URL 强制 HTTPS**，仅 `localhost` / `127.0.0.1` / `[::1]` 允许明文 HTTP。
  升级前保存的远端明文 HTTP 地址会被标记为不可用，需重新填写并保存。
- **同标签页内导航会让摘要标记为过期**（不再继续显示为当前页面的结果），并提供重新总结入口。
  不会自动重新总结，以免在你正常浏览时产生额外调用。

### 修复

- DeepSeek 流式总结遇到 401 时，刷新得到的新 token 现在会真正用于重试的授权头、重新求解的 PoW
  头以及会话删除；此前重试始终沿用旧 token，导致自动重试实际无效。
- 三处 SSE 解析实现统一为一处，并修复流结束时不处理缓冲区、导致**最后一帧没有换行时丢数据**的
  问题。
- 网络超时改为真正可触发：此前只在 `read()` 返回后比较时间戳，连接挂起时超时永远不会触发。现在
  空闲 120 秒无数据即中断；Kimi 文件解析等待（3 分钟）同样改为真正中断。

### 工程

- 单元测试任一失败或抛异常均返回非零退出码，新增权限基线、Kimi 清洗与体积上限、统一 SSE、
  凭据迁移、API Key 存储策略、DeepSeek 401 重试等回归用例。
- 新增 GitHub Actions CI（语法检查 + manifest 校验 + 单元测试）。
- 新增 `SECURITY.md`；`CODEBUDDY.md` 补充权限策略、SSE 与超时、凭据存储等说明。
- `tools/gen_icons.ps1` 不再写死本机路径：默认读取仓库根目录的 `logo.png`，可用 `-Source` /
  `-OutDir` 指定，缺省源图时给出提示并以非零码退出。

## 1.8.3 及更早

设置页仅 CSS 调整（单选框改为块状按钮），`DeepSeek` / `Kimi` / OpenAI 兼容 / Gemini 四种模式
与 B 站视频页总结能力均已具备。详见 git 提交历史。
